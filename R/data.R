#' Matrice de saturations factorielles par défaut pour FE_tests
#'
#' Matrice de saturations factorielles (11 variables × 3 facteurs) utilisée
#' comme valeur par défaut dans \code{\link{FE_tests}}. Les deux premières
#' variables (lignes 1–2) servent d'ancres sur le facteur 1 ; leurs saturations
#' sont remplacées dynamiquement lors des simulations.
#'
#' @format Une matrice numérique 11 × 3.
#' @source Défini dans \code{data-raw/matrice_EF.R}.
"matrice_EF"
